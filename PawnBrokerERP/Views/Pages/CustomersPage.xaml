<UserControl x:Class="PawnBrokerERP.Views.Pages.CustomersPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="{StaticResource BackgroundBrush}" Margin="32,28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Customers" Style="{StaticResource PageTitle}" Margin="0"/>
            <Border Grid.Column="1" Background="{StaticResource CardBrush}" CornerRadius="8"
                    Padding="16,8" VerticalAlignment="Top">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Total: " Foreground="{StaticResource TextSubBrush}" FontSize="13" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding TotalCustomers}" Foreground="{StaticResource AccentGoldBrush}"
                               FontSize="18" FontWeight="Bold"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Search -->
        <TextBox Grid.Row="1" Style="{StaticResource ModernTextBox}"
                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,0,16"/>

        <!-- Grid -->
        <DataGrid Grid.Row="2" Style="{StaticResource ModernDataGrid}"
                  ItemsSource="{Binding Customers}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="180"/>
                <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="120"/>
                <DataGridTextColumn Header="Aadhaar" Binding="{Binding AadhaarNumber}" Width="130"/>
                <DataGridTextColumn Header="Address" Binding="{Binding Address}" Width="*"/>
                <DataGridTextColumn Header="Total Pledges" Binding="{Binding Pledges.Count}" Width="100"/>
                <DataGridTextColumn Header="Since" Binding="{Binding CreatedAt, StringFormat=dd-MMM-yyyy}" Width="110"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</UserControl>
